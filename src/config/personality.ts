// Kept in one module so a future settings/memory layer can extend it. The native
// backend owns the authoritative copy sent to OpenAI, preventing client tampering.
export const ASSISTANT_NAME = "J.A.R.V.I.S.";

export const WELCOME_MESSAGE = "Sistema online. À disposição.";
